const defaultSpacing = 16;

export const Spacings = {
  default: `${defaultSpacing}px`, //16px
  xxs: `${0.25 * defaultSpacing}px`, // 4px
  xs: `${0.5 * defaultSpacing}px`, // 8px
  sm: `${0.75 * defaultSpacing}px`, // 12px
  md: `${1.25 * defaultSpacing}px`, // 20px
  lg: `${2 * defaultSpacing}px`, // 32px
  xl: `${3.25 * defaultSpacing}px`, // 52px
  xxl: `${5.25 * defaultSpacing}px`, // 84px
};
